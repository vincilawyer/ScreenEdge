# 项目约定

Apple 芯片 Mac 接续安装、当前版本状态和验收步骤见 [AI 维护说明](docs/ai-maintenance.md)。

- 默认直接在 `main` 上修改、检查并提交，使用提交记录保留迭代历史和回退点；不要覆盖用户或其他任务的修改。
- 只有用户明确要求在分支上进行时，才创建任务分支或配套 worktree；不要因任务规模或实验性质自行增加例外，也不默认创建 PR。经用户要求使用的临时分支，完成并按授权合并后及时清理。
- 原生 Swift / AppKit，无第三方运行时依赖。
- 只绘制鼠标穿透的覆盖层；不注入、不拦截、不移动键鼠，不修改系统显示器排列。
- 自动扩展屏、自动通用控制通道和手动补充标记必须明确区分；不可将手动标记声称为已连接状态。
- 通用控制只使用基本只读边缘接口；不要调用管理接口或改变系统设置。接口失败、空结果、停用时清除旧自动标记。
- 通用控制对端示意只用于设置预览，需明确标为示意；不可混入实际显示器或覆盖层坐标。
- 不将单端读取和绘制成功声称为已完成跨设备两端颜色映射实测。
- 提交前运行构建、几何逻辑检查和适当的 UI 验证，中文提交说明。
- 不提交构建产物、用户配置、真实设备标识或私人数据。
- 本项目已获所有者授权公开发布到 `vincilawyer/ScreenEdge`；推送和发布只限本项目，继续遵守当前任务的授权范围。
- Release 仅包含明确选择的应用构建和校验文件；本机配置、临时诊断和设备标识不得上传。
